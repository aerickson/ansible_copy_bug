---


- name: copy file
  copy: src=test_file dest=/etc/test_file owner=root group=root mode=644
  tags:
    - system

# TODO: make hard link
- name: make hard link
  file:
    path: /etc/test_file
    state: hard

- name: copy file 2
  copy: src=test_file dest=/etc/test_file owner=root group=root mode=644
  tags:
    - system
